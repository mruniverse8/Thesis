seed: 42

model:
  model_name_or_path: QizhiPei/biot5-plus-base-chebi20
  model_max_length: 512
  device: auto

data:
  train_file: data/chebi20/processed/train.jsonl
  staging_dir: data_collection/outputs/chebi20_biot5_train
  derived_train_file: data/post_training/processed/train_multimol.jsonl

generation:
  target_molecules_per_description: 30
  max_length: 512
  num_beams: 30
  num_return_sequences: 30
  num_beam_groups: 6
  diversity_penalty: 0.5
  early_stopping: true
  length_penalty: 1.0

filtering:
  acceptance_dice_threshold: 0.7
  fingerprint_radius: 2
  fingerprint_num_bits: 2048
  max_molecules_per_example: 8

runtime:
  description_offset: 0
  max_descriptions:
  num_parts: 1
  part_index: 1
